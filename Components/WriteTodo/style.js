import { FONT, FONT_BASE } from '../../Style'

export const ST = {
  input: {
    ...FONT_BASE,
    fontSize: FONT.size,
    color: FONT.color,
    height: 40,
    borderRadius: 10,
    borderColor: 'grey',
    borderWidth: 1,
    width: '90%',
    marginTop: 30,
    margin: 15,
    padding: 5,
    paddingHorizontal: 20,
  },
}
